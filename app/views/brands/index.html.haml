%h1 Brands

%table
  %tr
    %th Name
    %th.actions

  - @brands.each do |brand|
    %tr
      %td= brand.name
      = render 'shared/index_row_buttons', object: brand

%br

= link_to 'New Brand', new_brand_path, :class => 'button'
